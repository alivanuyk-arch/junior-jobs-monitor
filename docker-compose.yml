version: '3.8'
services:
  jobs-monitor:
    build: .
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - CHAT_ID=${CHAT_ID}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
